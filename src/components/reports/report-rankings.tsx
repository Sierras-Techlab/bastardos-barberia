"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import type { ReportDayHighlight, ReportRankedItem } from "@/types/report";
const money = new Intl.NumberFormat("es-AR", { style: "currency", currency: "ARS", maximumFractionDigits: 0 });
function Ranking({ title, items }: { title: string; items: ReportRankedItem[] }) { const [expanded, setExpanded] = useState(false); const shown = expanded ? items : items.slice(0, 5); return <Card className="bg-white"><CardHeader><CardTitle className="text-base">{title}</CardTitle></CardHeader><CardContent>{shown.length ? <div className="space-y-3">{shown.map((item, index) => <div key={item.id} className="flex items-center gap-3"><span className="w-5 text-xs text-muted-foreground">{index + 1}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{item.name}</p><p className="text-xs text-muted-foreground">{item.quantity} unidades</p></div><span className="text-sm font-semibold">{money.format(item.amount)}</span></div>)}</div> : <p className="text-sm text-muted-foreground">Sin actividad en este período.</p>}{items.length > 5 && <Button variant="ghost" className="mt-3 px-0" onClick={() => setExpanded(!expanded)}>{expanded ? "Ocultar detalle" : `Ver detalle de ${title.toLowerCase()}`}</Button>}</CardContent></Card>; }
export function ReportRankings({ services, products, highlights }: { services: ReportRankedItem[]; products: ReportRankedItem[]; highlights: { bestDay: ReportDayHighlight | null; worstDay: ReportDayHighlight | null } }) { return <section><div className="grid gap-4 lg:grid-cols-2"><Ranking title="Servicios" items={services} /><Ranking title="Productos" items={products} /></div><div className="mt-4 grid gap-3 sm:grid-cols-2">{[["Mejor día", highlights.bestDay], ["Peor día", highlights.worstDay]].map(([label, value]) => <div key={label as string} className="rounded-2xl border bg-white p-4"><p className="text-xs text-muted-foreground">{label as string}</p><p className="mt-1 font-semibold">{value ? money.format((value as ReportDayHighlight).amount) : "Sin actividad"}</p>{value && <p className="text-xs text-muted-foreground">{(value as ReportDayHighlight).date}</p>}</div>)}</div></section>; }
